[project]
name = "uvcond"
version = "0.2.3"
description = "Conda-like named envs on top of uv"
requires-python = ">=3.9"
dependencies = [
    "tomli; python_version < '3.11'",
]

[project.scripts]
uvcond = "uvcond.__main__:main"
